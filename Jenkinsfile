pipeline {
    agent any
    stages {
        stage("checkout") {
            steps {
                echo "build the pipeline"
            }
        }

        stage("test") {
            steps {
                echo "build the pipeline"
            }
        }

        stage("build") {
            steps {
                echo "build the pipeline"
            }
        }
    }
}
